---
const { kicker, title, intro } = Astro.props;
---

<header class="section-intro">
  {kicker ? <p class="section-kicker">{kicker}</p> : null}
  <h2 class="section-heading">{title}</h2>
  {intro ? <p class="section-copy">{intro}</p> : null}
</header>
